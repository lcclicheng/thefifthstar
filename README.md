# Fifth Star

Landing page for **Fifth Star** — AI-written Google review replies for UK local businesses.

- Single, self-contained `index.html` (no build step).
- Fonts via Google Fonts CDN; favicon is an inline SVG.
- Deployed to GitHub Pages from the `main` branch (root).

## Local preview

Open `index.html` directly, or:

```bash
python -m http.server 8000   # then visit http://localhost:8000
```

## Repo

`lcclicheng/thefifthstar`
